export default function Production() {
  return (
    <div className="stack">
      <h1>Production</h1>
      <div className="card">
        <p className="muted">
          Placeholder. This is where station-specific entry forms will live — one form
          per station, writing to <code>record_production</code> (and{' '}
          <code>record_session</code> / <code>record_photo</code> for the throughput
          stations).
        </p>
      </div>
    </div>
  )
}
